{
  "version": "3.4.1",
  "releaseDate": "2026-05-31",
  "changelog": "Photos taken on your phone now show up on desktop automatically (and vice-versa). The cloud doc carries a tiny photo signature (id + uploaded flag) so the other device knows when new photos exist and pulls them from Storage — while staying small enough to avoid the 1MB limit. Sorted signature prevents sync loops."
}
